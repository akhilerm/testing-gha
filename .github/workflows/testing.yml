name: CI
on:
  push:
    branches:
      - ubuntu-24-testing

jobs:
  test-default-containerd:
    name: Test Default containerd
    runs-on: ubuntu-24.04

    steps:
      - name: Version check containerd
        run: containerd --version
      - name: Which
        run: which containerd
      - name: Which runc
        run: which runc
      - name: Version check runc
        run: runc --version
