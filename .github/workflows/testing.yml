name: CI
on:
  push:
    branches:
      - ubuntu-24-testing

jobs:
  test-default-containerd:
    name: Test Default containerd
    runs-on: ubuntu-24.04

    steps:
      - name: Version check containerd
        run: containerd --version
      - name: Version check runc
        run: runc --version
